<sqlMap namespace="NPXC001001CreateApprovalHistory">
    <statement id="getFullPsnNm" parameterClass="Map" resultClass="String">
        SELECT
             CASE WHEN AP.MID_NM IS NULL THEN 
                 AP.FIRST_NM || ' ' || AP.LAST_NM
             ELSE 
                 AP.FIRST_NM || ' ' || AP.MID_NM || ' ' || AP.LAST_NM
             END AS FULL_PSN_NM
        FROM
             AUTH_PSN AP
        WHERE
            AP.GLBL_CMPY_CD  = #glblCmpyCd#
        AND AP.USR_NM        = #psnCd#
    </statement>
</sqlMap>
