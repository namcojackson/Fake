<!--<?xml version="1.0" encoding="UTF-8"?>-->

<sqlMap namespace="NLXC008001Query">

    <statement id="checkSerialEvent" parameterClass="Map" resultClass="Map">
		SELECT SER_OWNR_ID
		FROM (
		  SELECT   SES.SER_OWNR_ID
		  FROM     SER_EVENT_STS SES
		  WHERE    SES.GLBL_CMPY_CD  = #glblCmpyCd#
		  AND      SES.EZCANCELFLAG  = '0'
		  AND      SES.MDSE_CD       = #mdseCd#
		  AND      SES.SER_NUM       = #serNum#
		  ORDER BY SES.SER_EVENT_TS DESC
		) WHERE ROWNUM = 1
    </statement>

</sqlMap>
