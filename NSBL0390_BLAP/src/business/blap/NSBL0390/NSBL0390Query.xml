<?xml version="1.0" encoding="UTF-8" standalone="no"?>

<sqlMap namespace="NSBL0390Query">

<statement id="getSvcModByModSqNmNextVal" parameterClass="Map" resultClass="String">
    SELECT
        LPAD(NVL(MAX(SM.SVC_MOD_SQ_NUM), 0) + 1, 4, 0)
    FROM
        SVC_MOD SM
    WHERE
            SM.GLBL_CMPY_CD = #glblCmpyCd#
        AND SM.SVC_MOD_YR   = #svcModYr#
        AND SM.SVC_MOD_MTH  = #svcModMth#
        AND SM.SVC_MOD_DAY  = #svcModDay#
        AND SM.SVC_MNF_CD   = #svcMnfCd#
        AND SM.EZCANCELFLAG = 0
</statement>

</sqlMap>
