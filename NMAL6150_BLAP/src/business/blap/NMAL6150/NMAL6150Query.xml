<?xml version="1.0" encoding="UTF-8" standalone="no"?>

<sqlMap namespace="NMAL6150Query">

    <statement id="getStList" parameterClass="Map" resultClass="Map">
        SELECT 
               ST_CD
             , ST_NM
          FROM 
               ST
             , GLBL_CMPY GC
         WHERE 
               ST.EZCANCELFLAG = '0'
           AND ST.GLBL_CMPY_CD = #glblCmpyCd#
           AND GC.EZCANCELFLAG = '0'
           AND ST.GLBL_CMPY_CD = GC.GLBL_CMPY_CD
           AND ST.CTRY_CD = GC.CTRY_CD
         ORDER BY
               ST.ST_SORT_NUM
    </statement>
 
</sqlMap>
